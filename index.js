// import inquirer package
const inq = require("inquirer");

// global var


// init func 
let init = () => {
    // inquirer prompt
    inq
        .prompt([
            {

            }
        ])
        // then response
        .then((response) => {
        });
}

// call init 
init();